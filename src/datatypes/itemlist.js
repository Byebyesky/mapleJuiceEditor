class item {
    constructor(id, amount, isNew) {
        this.id = id;
        this.amount = amount;
        this.isNew = isNew;
    }
}

class itemlist {
    constructor(size, items) {
        this.size = size;
        this.items = items;
    }
}